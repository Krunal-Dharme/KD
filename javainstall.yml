---
- hosts: mywebservers
  become: yes
  tasks:
  -  name: I want to install java on ubuntu node.
     apt:
      name:  "{{myjavawebpackonubuntu}}"
      service: present
  -  name: I want to install java on top of d next ubuntu node.
     apt:
       name: "{{myjavawebpackonubuntu}}" 
       service: present